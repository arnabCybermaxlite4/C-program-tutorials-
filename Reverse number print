#include <stdio.h>

int main() {
    int i, j, num = 25;
    for(i = 1; i <= 5; i++) {
        for(j = 1; j <= 5; j++) {
            printf("%2d ", num--);
        }
        printf("\n");
    }
    return 0;
}
